# Auth0 backend credentials — set these as environment variables in your hosting dashboard
# API registered in Auth0 dashboard:
#   Identifier (audience): https://partneriq.api
#   Signing Algorithm:     RS256

AUTH0_DOMAIN=disruptready.us.auth0.com
AUTH0_AUDIENCE=https://partneriq.api

# CORS — your production domain
ALLOWED_ORIGINS=https://partneriq.pellerintechnology.com,http://localhost:5173

# Federal data API keys (optional — public endpoints work without them)
BLS_API_KEY=
CENSUS_API_KEY=
